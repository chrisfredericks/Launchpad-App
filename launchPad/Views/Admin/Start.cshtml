@model launchPad.Models.LinkManager;

@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
  <div class="row">
    <div class="col-sm">
      <h2>@Model.categories[0].category<h2>
    </div>
    <div class="col-sm">
      <h2>@Model.categories[1].category<h2>
    </div>
    <div class="col-sm">
      <h2>@Model.categories[2].category<h2>
    </div>
    <div class="col-sm">
      <h2>@Model.categories[3].category<h2>
    </div>
  </div>
</div>
